<%= well 'category', pad: false do %>
   <table class="table table-striped">
   <thead>
      <tr>
         <th>Name</th>
         <th></th>
      </tr>
   </thead>
   <tbody>
      <%= render partial: 'category', collection: @categories %>
   </tbody>
</table>
<div class="center">
  <%= link_to 'add new', new_category_path %>
</div>

<% end %>
